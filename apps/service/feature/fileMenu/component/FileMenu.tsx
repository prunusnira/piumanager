'use client';

import { Button } from '@/common/button/Button';

export const FileMenu = () => {
    return (
        <>
            <Button text={'newuser'}></Button>
            <Button text={'load'}></Button>
            <Button text={'save'}></Button>
        </>
    );
};
