import { Button } from '@/common/button/Button';

export const TableButton = () => {
    return (
        <>
            <Button text={'scrshot'} />
            <Button text={'share'} />
        </>
    );
};
