import { Button } from '@/common/button/Button';

export const UserMenu = () => {
    return (
        <>
            <Button text={'edit'} />
            <Button text={'updateCheckBtn'} />
            <Button text={'checkDisplay'} />
            <Button text={'rankDisplay'} />
        </>
    );
};
